# Timeline Service

## Setup
Run Redis and the App:
```bash
docker compose up
```

Run tests:
```bash
virtualenv venv
source venv/Scripts/activate
pip install -r requirements.txt
python -m unittest
```
